<%= pb_form_with(model: routine, local: true) do |form| %>
  <% if routine.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(routine.errors.count, "error") %> prohibited this routine from being saved:</h2>

      <ul>
        <% routine.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <%= form.text_field :name, props: { label: true } %>

  <%= form.actions do |action| %>
    <%= action.submit %>
  <% end %>
<% end %>
